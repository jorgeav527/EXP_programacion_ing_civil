## HTML CSS 66 JS
### HTML 
- Enpesemos por HTML es el archivo que le da estructura a nuestra aplicacion llamado HyperText Markup Language este archivo contiene dos partes escenciales el la head o cabeza del documento y el body o cuerpo del documento, vamos a abrir el documento en un editor de texto...
#### Que es un editor de texto?
Es un software para crear y modificar archivos digitales de texto plano que son sumamente livianos y faciles de ejecutar. Los editores usados más usados son Atom, SublimeText, NotePad++ solo para windows, Vim que es el editor de mi preferencia pero usaremos VSCode que fue recientemente comprado por Microft
... como podemos ver aqui hay una estructura de componentes padre e hijos donde se puede ver que *title* está dentro de *head*, *heading1*, *heading2* junto con *paragraph* h1 h2 y p repectivamente estan dentro de *body* y tanto *head && body* estan dentro de html toda esta estructura la conforma la DOM o Document Object Model
- procegiremos con ver los diferentes elementos h1, h2 ... hasta el h6 los parafos, dos tipos de listas unorder list y order list, podemos incertar imagenes con atributos como *souce* src o donde esta la fuente del archivo, las dimenciones como alto, ancho o crear una imagen dentro de un link y referenciarlo a nuestro mejor amigo google mediante el atributo *hyperreference* 
- Como vemos los elementos de HTML tienen un patron muy similar con atributos si asi lo requiere nuestra aplicacion para dar un contexto y estrucctura sobre como el browser deberia de representar el documento.
- Ahora como esto me ayuda en mi Ing Civil bueno vamos a dar un salto y usar estas herramientas para hacer una simple tabla del ensayo de "rotura de probetas de concreto" esta tabla tiene los datos que estan pre calculados. Dentro de table esta thead y dentro esta la fila donde mostramos los titulos de cada columna por otro lado tenemos tbody donde dentro de cada fila vemos los datos de de las probetas A y B.
- Ahora haremos una forma para introducir información el nombre de la probeta, el diametro, y la fuerza de rotura, luego un boton de submit para cuando este llena enviemos esta data, para luego usando tanto javascript como python procecen los datos en el servidor y luego nos envie las respuestas pudiendo visualizar el resultado.
- Con CSS vamos a darle un poco de forma, color y estilo a cada uno de los elementos de nuestros documentos html, primero al un poco al index creando un archivo llamado index-estilo.css donde le importamos una fuente de letra le damos propiedades a los elementos h1, h2, h3, h4: como el color, los margenes entre cada uno de ellos, las sombras del texto el ancho que tomara el elemto el contenido que se le da al comienzo y al final de cada linea la fuente, entre otras propiedades, lo mismo para la tabla que creamos antes. Para el archivo forma creamos un archivo estilo.css y lo conectamos de la siguiente forma-estilo.css.
- Para no volver a inventar la rueda y enfocarnos en el la solucion de problemas tenemos librerias que nos ayudan a dar elito a nuestras interfaces son diversas y cada una de ellas tiene cualidades propias como *bootstrap* para utilizar *bootstrap* todo lo que necesitamos es copiar el link en la parte de *head* de nuestro documento forma.html y de esa manera tenemos lo siguiente aparte de darnos un variedad de diseños hace que nuestra paguina sea *movilfriendly* lo que significa que se adapta a cualquier dispositivo.

- Siguiente el lenguaje de programamcion de los navegadores comunmente usuario llamado cliente, en otras palabras vamos a poder ejecutar codigo que directamente va a modificar el contenido que acede o requiere el usuario como el seleccionar algo, hacer clic en algun objeto o hacer scroll, en otras palabras el navegador funciona con eventos ... vamos a revisar el lenguaje de programacion javascript junto con un ejemplo simple de ingenieria civil, usando nuestra "forma" para interactuar con el navegador y nuestra "tabla" para mostrar los resultados le daremos una funcion al boton de calcular, en otras palabras calcularemos el esfuerzo de rotura de la probeta utilizando el diametro para calcular el area y la fuerza de rotura de una maquina para calcular el esfuerzo.
- primero que nada crearemos un archivo func.js y lo conectaremos mediante el tag script y el atributo src indicando el archivo recientemente creado y lo introduciremos al final del body de nuestro archivo forma.html dentro hacemos una prueva para ver si estan conectados mediante el comando *console.log* "Hola Civiles". Ahora crearemos una variable estas se crean declarandolas con los comandos let para un valor variable y cons para asignar un valor cosntante a esta la llamaremos pi y haciendo uso del modulo *Math* o matematica e invocando a PI, con el comando *console.log* mostraremos nuestra constante pi: como podemos ver en la consola del navegador nos muestra el resultado.
-Ahora identificamos los elementos mediante *document.querySelector* segido del argumento #probetas, #diametro y #fuerza que fueron los identificadores unicos id que le dimos en nuestro archivo forma.html "*document.querySelector* es una funcion que requiere un identificador".
- Ahora mediante el mismo comando pero ahora identificando a nuestra forma tomaremos la propiedad cuando se active el boton de submit escribiremos la siguiente funcion primero que nada cuando demos al boton de calcular extraeremos los datos que el usuario escribio y asignaremos a cada uno una variable calculamos en area de la circunferencia en base al diametro de la probeta, luego encontramos el efuerzo que es la fuerza sobre el area y asignamos los resultados a la variable area y esfuerzo respectivamente luego como ingresamos los resultados en la tabla, bueno una forma sencilla de realizar esto sin ir a los detalles pero que funcione podria ser la siguiente, creamos una variable de nueva-fila la cual contendra un pequeño html de los datos de una fila de la tabla que creamos respetando el orden de los titulos el simbolo $ seguido de los {} es para ingresar las variables de los datos calculados el nombre de la probeta, el diametro, el area y la fuerza donde agregaremos una propiedad que tienen los numeros que es ajustar el numero de decimales a por ejemplo 3 la fuerza para por ultimo indicar que a la variable que fue asignada para contener mi tabla agregarle la varible nueva-fila mediante el comando innerHTML esto sera tabla.innerHTML =  a lo que ya este en tabla.innerHTML + nueva-fila esto sera de simplificado asi. Listo!
- En este ejemplo mostre como atrasves de comandos rapidos y varibles sencillas a resolver un problema sencillo de rotura de probetas. 
